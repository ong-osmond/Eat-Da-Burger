<li>
	{{burger_name}}
	<button class="change-devoured boxed-btn3" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}I want another!{{else}}Devour it!{{/if}}
	</button>
	<button class="delete-burger boxed-btnRemove" data-id="{{this.id}}">Remove</button>
</li>




